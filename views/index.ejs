<!DOCTYPE html>
<html>
    <head>
        <title>Tipuri de flori</title>
        <meta charset="UTF-8"/>
        <link rel="stylesheet" href="design.css"/>
        <script src="index.js" defer></script>
    </head>
    <body>
        <div class="container">
            <div class="title">Floarea ta preferata:</div>
            <div class="items">
                <%
                result.map(m => m._doc).forEach((item) => {
                    %>
                    <div class="item" onclick="showPupUp('<%=item['nume floare']%>', `<%=item.descriere%>`)">
                        <img src="<%=item['imagine ']%>" alt="flowers"/>
                        <p><%=item['nume floare']%></p>
                    </div>
                    <%
                })
                %>
            </div>
        </div>
        <div class="popup">
            <h2></h2>
            <a class="close" href="#" onclick="hidePopUp()">&times;</a>
            <div class="content"></div>
        </div>
    </body>
</html>